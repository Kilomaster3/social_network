.mb-3
  = link_to new_room_path, class: 'btn btn-primary' do
    Create chat
- if @chats.present?
  %nav.nav.flex-column
    - @chats.each do |chat|
      = link_to chat.name, room_path(chat), class: 'nav-link room-nav-link'
- else
  .text-muted
    The are no chat
