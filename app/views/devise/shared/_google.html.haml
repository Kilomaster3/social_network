- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    %a.btn.btn-danger{href: omniauth_authorize_path(resource_name, provider)}
      %span.hidden-xs Google+

